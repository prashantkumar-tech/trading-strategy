"""Live portfolio operations layer.

A persistent paper-portfolio state model (``state``), a JSON-backed store
(``store``), and the live rotation/rebalance logic (``engine``) built on top of
the existing momentum engine in ``backtest.momentum_rotation``.
"""
